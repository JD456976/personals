models:
  Page:
    title: string
    slug: string
    content: longtext
    active: boolean
    main_menu: boolean
    footer_menu: boolean
    timestamps

controllers:
  Admin/Page:
    resource

seeders: Page
